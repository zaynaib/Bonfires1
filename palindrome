function palindrome(str) {
  // Good luck!
 
  str = str.toLowerCase();
  str = str.replace( /[\s,.]/g, "");
  console.log(str);
  str2 = str.split('').reverse().join('');
  console.log(str2);
  
  if(str == str2)
    return true;
  else
    return false;
  }
